## Database Setup
1. Open phpMyAdmin.
2. Create a new database (name as `event` or as you prefer).
3. Import the file `database/event.sql`.
4. Update database credentials in `connection.php`.
5. Run the project.
